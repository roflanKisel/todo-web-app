import styled from 'styled-components';

const FormTitle = styled.h3`
  padding: 0;
  margin: 0;
  margin-bottom: 24px;

  font-size: 40px;
  font-weight: 600;

  justify-self: center;
`;

export default FormTitle;
